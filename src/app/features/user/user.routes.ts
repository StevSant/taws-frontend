import { Routes } from '@angular/router';
import { UserPageComponent } from './presentation/user-page/user-page.component';

export const USER_ROUTES: Routes = [{ path: '', component: UserPageComponent }];
